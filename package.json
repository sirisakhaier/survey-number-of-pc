{
  "name": "survey-app",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-user Survey Web Application with Cloudflare Workers API, D1, and GitHub Actions",
  "main": "src/worker/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init:local": "wrangler d1 execute survey_db --local --file=./migrations/0001_schema.sql && wrangler d1 execute survey_db --local --file=./migrations/0002_seed.sql",
    "d1:init:remote": "wrangler d1 execute survey_db --remote --file=./migrations/0001_schema.sql && wrangler d1 execute survey_db --remote --file=./migrations/0002_seed.sql",
    "build": "tsc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240101.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.25.0"
  }
}
